# Everything the build writes, packed themes included: build/dist holds the
# .qbtheme files, build/{qss,icons,pack} the intermediates.
build/
*.qbtheme

# Stray intermediates, in case anything is ever written outside build/
*.qss
*.qrc
src/nova-dark/source/build-entry.scss

# Python
__pycache__/
*.pyc
